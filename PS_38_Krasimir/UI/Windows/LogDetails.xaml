<Window x:Class="UI.Windows.LogDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Log Entry Details" Height="250" Width="400" 
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="--- Log Event Details ---" HorizontalAlignment="Center" FontWeight="Bold" FontSize="14" Margin="0,0,0,15"/>
            <Separator Margin="0,0,0,10"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <TextBlock Text="{Binding SelectedLog.Timestamp, StringFormat='Timestamp: {0}'}" Margin="0,5"/>
            <TextBlock Text="Message:" FontWeight="SemiBold" Margin="0,5,0,2"/>
            <TextBlock Text="{Binding SelectedLog.Message}" TextWrapping="Wrap" Margin="0,0,0,0" RenderTransformOrigin="-0.024,0.791"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Save to File" 
                    Command="{Binding SaveToFileCommand}" 
                    Width="100" Height="30" Margin="5"/>
            <Button Content="OK" 
                    Click="Close_Click" 
                    Width="80" Height="30" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
